import 'package:flutter/material.dart';

class AppIcons {
  
  Icon play = const Icon(
    Icons.sports_esports_outlined,
    color: Colors.white,
  );

  Icon add = const Icon(
    Icons.add, 
    color: Colors.white,
  );

  Icon cards = const Icon(
    Icons.play_for_work_rounded,
    color: Colors.white,
  );
}